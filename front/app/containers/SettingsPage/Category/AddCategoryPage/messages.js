/*
 * AddCategoryPage Messages
 *
 * This contains all the text for the AddCategoryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddCategoryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddCategoryPage container!',
  },
});
