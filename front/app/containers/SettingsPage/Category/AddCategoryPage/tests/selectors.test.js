// import { selectAddCategoryPageDomain } from '../selectors';

describe('selectAddCategoryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
