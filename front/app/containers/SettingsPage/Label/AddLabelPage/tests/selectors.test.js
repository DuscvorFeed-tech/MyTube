// import { selectAddLabelPageDomain } from '../selectors';

describe('selectAddLabelPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
