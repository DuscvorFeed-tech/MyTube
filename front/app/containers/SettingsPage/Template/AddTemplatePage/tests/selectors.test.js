// import { selectAddTemplatePageDomain } from '../selectors';

describe('selectAddTemplatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
