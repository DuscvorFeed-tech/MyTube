// import { selectFormsDetailPageDomain } from '../selectors';

describe('selectFormsDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
