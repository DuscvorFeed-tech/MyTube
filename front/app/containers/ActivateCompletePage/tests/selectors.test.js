// import { selectActivateCompletePageDomain } from '../selectors';

describe('selectActivateCompletePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
