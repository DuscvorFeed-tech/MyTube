// import { selectSubmitSuccessPageDomain } from '../selectors';

describe('selectSubmitSuccessPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
