// import { selectEditCampaignPageDomain } from '../selectors';

describe('selectEditCampaignPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
