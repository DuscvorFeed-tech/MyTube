// import { selectCreateCampaignPageDomain } from '../selectors';

describe('selectCreateCampaignPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
