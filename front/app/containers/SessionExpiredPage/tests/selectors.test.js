// import { selectSessionExpiredPageDomain } from '../selectors';

describe('selectSessionExpiredPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
