// import { selectPublishedPageDomain } from '../selectors';

describe('selectPublishedPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
