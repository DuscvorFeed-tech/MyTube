// import { selectConfirmEmailPageDomain } from '../selectors';

describe('selectConfirmEmailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
