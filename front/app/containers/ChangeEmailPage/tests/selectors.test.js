// import { selectChangeEmailPageDomain } from '../selectors';

describe('selectChangeEmailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
