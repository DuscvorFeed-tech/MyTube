// import { selectComponentPageDomain } from '../selectors';

describe('selectComponentPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
