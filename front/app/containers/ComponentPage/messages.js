/*
 * ComponentPage Messages
 *
 * This contains all the text for the ComponentPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ComponentPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ComponentPage container!',
  },
});
