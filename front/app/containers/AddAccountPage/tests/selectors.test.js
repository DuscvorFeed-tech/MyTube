// import { selectAddAccountPageDomain } from '../selectors';

describe('selectAddAccountPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
