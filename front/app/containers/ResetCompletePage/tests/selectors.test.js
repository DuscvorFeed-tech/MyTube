// import { selectResetCompletePageDomain } from '../selectors';

describe('selectResetCompletePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
