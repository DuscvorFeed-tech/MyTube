// import { selectCampaignDetailPageDomain } from '../selectors';

describe('selectCampaignDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
