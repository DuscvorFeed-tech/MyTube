/*
 * ReactTimeline Messages
 *
 * This contains all the text for the ReactTimeline component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReactTimeline';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReactTimeline component!',
  },
  campaignTitle: {
    id: `campaignTitle`,
  },
});
